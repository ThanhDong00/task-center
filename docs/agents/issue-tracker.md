# Issue tracker: GitHub

Issues live in this repo's GitHub Issues (`github.com:ThanhDong00/task-center`). Uses `gh` CLI (`gh issue create` / `gh issue list`).

## PRs as a request surface
Off by default. Flip this flag if external PRs should enter the triage queue.
