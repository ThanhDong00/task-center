# Domain Docs

Single-context repo (most repos). Read before exploring:

- `CONTEXT.md` at repo root (create when first domain term resolves; `domain-modeling` creates lazily)
- `docs/adr/` for system-wide ADRs; also `src/<context>/docs/adr/` if multi-context later

Consumer rules: use glossary from `CONTEXT.md`; flag ADR contradictions explicitly; proceed silently if files missing.
